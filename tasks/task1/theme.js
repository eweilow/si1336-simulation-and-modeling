import theme from "mdx-deck/themes";

export default {
  // extends the default theme
  ...theme,
  // add a custom font
  // custom colors
  colors: {
    text: "#6200ff",
    background: "#fff",
    link: "#0ff"
  }
};
